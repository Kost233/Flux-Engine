#include <iostream>
#include <string>

int main() // This is where the game starts!  (OnStart)
{
    std::string message = "Hello World!";
    std::cout << message << std::endl;

    while (true) // This is where the game updates!  (OnUpdate)
    {
        // Put looping logic here!
    }

    return 0;
}